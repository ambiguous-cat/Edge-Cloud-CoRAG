# 本地启动前端

前端项目位于 `react-frontend/`。

```powershell
cd react-frontend
nvm use 20
npm install
npm run dev
```

启动成功后，在浏览器打开：

```text
http://localhost:5173
```

如果已经安装过依赖，后续只需要执行：

```powershell
cd react-frontend
nvm use 20
npm run dev
```
